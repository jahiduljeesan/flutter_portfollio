{
  "name": "portfollio",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable; fi && ./flutter/bin/flutter build web --release"
  }
}
